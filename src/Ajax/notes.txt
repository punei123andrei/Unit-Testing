- build request method ( appendParam should be integrated along with it )
- build request chaching method using transients api with wordpress class adesion
- separate verify nonce method and tie it to the wordpress core ( i.e. class     existence )
- 

- make two classes, one with a get request and one with a send request with methods and configurations, 
- integrate request caching and request method in these caching along with verify nonce
- use the class as a callback for addAjaxAction callback
- begin with the mapping of the registerRequest method, making an iterable class from the WP_request interface 

- probabily send data should be responsible for just send the data as a standalone class 

- think of the file structure

- return post data should be its own class ( class mapper ) folder /mapping

- behavioural patttern